<div class="row">
  <div class="col-xs-12">              
    <div class="box">
      <div class="box-header text-center">
        <h3 class="box-title">Lista de Articulos</h3>
      </div><!-- /.box-header -->
      <div class="box-body">
        <table id="example1" class="table table-bordered table-striped">
          <thead>
            <tr>
              <th>Fecha</th>
              <th>Subtotal</th>
              <th>Total</th>
              <th>Cliente</th>
              <th>Usuario</th>
              <th>Estado</th>
              <th>Acciones</th>
            </tr>
          </thead>
          <% @sales.each do |sale| %>
          <tbody>
            <tr>
              <td><%= sale.fecha %></td>
              <td><%= number_to_currency(sale.subtotal) %></td>
              <td><%= number_to_currency(sale.total) %></td>
              <td><%= sale.client %></td>
              <td><%= sale.user %></td>
              <td><%= sale.estado %></td>
              <td><%= number_to_currency(product.precio) %></td>
              <td><%= link_to "<button type='button' class='btn bg-purple3 btn-gradient btn-sm'> <span class='fa fa-pencil fa-2x'></span> </button>".html_safe, edit_sale_path(sale) %>
              <%= link_to "<button type='button' class='btn bg-purple3 btn-gradient btn-sm'> <span class='fa fa-trash fa-2x'></span> </button>".html_safe, sale_path(sale), method: :delete, data: {confirm: 'Esta Seguro de Continuar con la Acción?'} %>
              </td>
            </tr>           
          </tbody>
          <% end %>                   
        </table>
      </div><!-- /.box-body -->
    </div><!-- /.box -->
  </div><!-- /.col -->
</div><!-- /.row -->